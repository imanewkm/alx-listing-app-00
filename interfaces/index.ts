export interface CardProps {
    // TODO: Define props for Card component
}

export interface ButtonProps {
    // TODO: Define props for Button component
}

export interface PropertyProps {
    id: string;
    name: string;
    address: string;
    rating: number;
    category: string;
    price: number;
    offers?: string[];
    image?: string;
    description?: string;
}